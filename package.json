{
  "name": "oplaunch",
  "version": "1.0.0",
  "private": true,
  "description": "OpLaunch - DeFi Launchpad + DEX + Staking on Bitcoin (OP_NET)",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "cd contracts && npm install && cd ../shared && npm install && cd ../backend && npm install && cd ../frontend && npm install",
    "build:contracts": "cd contracts && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
